name: astrbot_plugin_no_ai_on_non_commands # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 你不许调用 AI # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 在没有对应命令的情况下，阻止检测到 / 开头的消息时调用 AI # 插件简短描述
version: v1.0.1 # 插件版本号。格式：v1.1.1 或者 v1.1
author: GrassBlock1 # 作者
repo: https://github.com/GrassBlock1/astrbot_plugin_no_ai_on_non_commands # 插件的仓库地址
